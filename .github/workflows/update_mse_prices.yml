name: Update MSE Prices

on:
  workflow_dispatch:
  schedule:
    # Every 30 minutes Mon–Fri (UTC).
    - cron: "*/30 7-14 * * 1-5"

jobs:
  scrape-and-update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r tools/scraper/requirements.txt

      - name: Update stock prices (MSE mainboard)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: python tools/scraper/mse_mainboard_prices.py

      - name: Trigger price alerts -> notifications
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: python tools/scraper/trigger_price_alerts.py

      - name: Update market news (MSE announcements)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: python tools/scraper/mse_announcements_scraper.py
